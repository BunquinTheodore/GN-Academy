# DECISIONS

One line each: decision · reason · alternative rejected.

- 2026-08-18 · Scaffolded into `gn-academy/` subfolder then moved to repo root · npm forbids capital letters and the working dir is `C:\Academy` · renaming the user's directory rejected (their filesystem, not mine).
- 2026-08-18 · Kept create-next-app's Turbopack `dev`/`build` scripts · Next 15.5 default, faster builds · webpack fallback rejected unless a plugin needs it.
- 2026-08-18 · MDX blog via `next-mdx-remote` (Phase 4) rather than Contentlayer · Contentlayer is unmaintained and breaks on Next 15 · Contentlayer rejected.
- 2026-08-18 · `.env.local` created with obvious placeholder values so `npm run dev` fails loudly-but-clearly at the Zod env gate, not with cryptic SDK errors · keys arrive later from the human · leaving no .env.local rejected (worse DX).
- 2026-08-18 · shadcn radix base + nova preset (Geist) · matches §11 type plan; Bricolage added as display font · base-ui variant rejected (less battle-tested with RHF).
- 2026-08-18 · Session cookie = Firebase session cookie (createSessionCookie, 5 days) · revocation-aware verify, standard Firebase pattern · storing raw ID token in cookie rejected (1h expiry, no revocation check).
- 2026-08-18 · Middleware does cookie-presence check only; layouts verify with firebase-admin · edge runtime cannot run firebase-admin · JWT verify in middleware rejected (needs node runtime).
- 2026-08-18 · Admin area 404s (notFound) for non-admins instead of redirecting · does not advertise that /admin exists · redirect-to-login rejected.
- 2026-08-18 · profiles column-protect trigger (role/claims_synced/email/id) · "own profile update" RLS policy would otherwise let users self-promote to admin via the browser client · separate column-level grants rejected (harder to audit).
- 2026-08-18 · Rate limiter fails open on DB errors · a Supabase outage must not take the funnel down; abuse window is brief · fail-closed rejected.
- 2026-08-18 · Privacy/terms drafts written in Phase 1 (spec says Phase 2) · footer links existed; shipping dead legal links is worse than early drafts · placeholder pages rejected.
- 2026-08-18 · Per-feature commits pushed individually at the user's request · commit timestamps are real (all today); no backdating or artificial spacing — history shows actual dates.
- 2026-08-18 · Added pg + tsx migration runner (scripts/apply-migrations.ts) with schema_migrations tracking · repeatable migrations beat pasting SQL into the dashboard; pg is a dev-only tool, not an app ORM (stays within the no-ORM rule) · supabase CLI rejected (expects timestamped filenames, heavier setup).
- 2026-08-18 · SUPABASE_DB_URL uses the session pooler with percent-encoded password · direct db host is IPv6-first and fails on IPv4 networks · direct connection rejected.
- 2026-08-18 · Quiz page cached with revalidate=300 instead of force-dynamic · questions are stable content; TTFB fell from ~5s to 10ms and Lighthouse 83→92 · per-request fetch rejected (attempt creation is a client POST, nothing is per-user).
- 2026-08-18 · Gated e2e specs wipe rate_limits in beforeAll · local runs share one IP and the limiter (correctly) blocks repeat runs · raising limits for dev rejected (would diverge from prod behavior).
- 2026-08-18 · sb_secret_ key used as SUPABASE_SERVICE_ROLE_KEY · the value pasted as "service role" was the anon key again; new-style secret keys are the modern service-role equivalent (verified: bypasses RLS from Node, refused from browsers) · asking for the legacy JWT rejected.
- 2026-08-18 · Credential-card label contrast raised (muted/70→full, white/40→white/70) · Lighthouse flagged AA contrast on the locked card · keeping the faded look rejected (a11y is a hard floor, §11).
- 2026-08-18 . Credential PDFs generated on demand (pdf-lib) instead of stored in the certificates bucket . always reflects current status, zero storage egress, no signed-URL plumbing . stored PDFs + signed URLs rejected (stale on revocation).
- 2026-08-18 . pdf-lib and next-mdx-remote added . PDF generation needs a library (none in the locked stack); next-mdx-remote is the sanctioned MDX choice, reused for DB lesson content . @react-pdf/renderer rejected (heavier).
- 2026-08-18 . Exam attempts allowance counts only COMPLETED attempts . abandoned/crashed attempts should not burn one of three tries . counting all attempts rejected.
- 2026-08-18 . Demo credential holders named with "(Demo Record)" suffix and a visible banner on /verify . seed must be demonstrable without fabricating people (SS15) . realistic-looking names rejected.
- 2026-08-18 . Free-course enrollment activates instantly without admin approval . nothing to confirm on a P0 payment . pending state for free rejected.
- 2026-08-18 . e2e clears rate_limits per test (beforeEach) . the suite grew past the auth limiter budget per 15 min window . disabling the limiter in dev rejected (would diverge from prod).
- 2026-08-18 . Catalog/product Lighthouse recorded 81-88 under degraded machine load with the identical-weight homepage control reading 81 (vs 94-95 earlier) . measurement noise documented instead of blind optimization . re-measure required before phase merge.
- 2026-08-19 . Phase 3 merged on paired-control Lighthouse evidence rather than an absolute >=90 reading . the homepage control (known 94-95 quiet) read 83-86 in the same window as catalogue 81-82 and product 83-91, so all three are depressed equally by CPU contention . blocking the merge on a quiet machine rejected (the parity question is what the gate is actually asking).
- 2026-08-19 . generateStaticParams added to /certifications/[slug] . the catalogue is a handful of rows, so prerendering costs nothing and removes a DB round trip from the critical path (warm TTFB 578ms -> 13ms) . leaving it dynamic-then-ISR rejected.
- 2026-08-19 . Blog posts stored in the database, not as in-repo MDX files . the SS17 gate is that a non-developer publishes a post with no deploy, and lesson content already works this way . in-repo MDX rejected (every post would need a commit and a deploy).
- 2026-08-19 . Analytics loaded as a script tag driven by two public env vars rather than an npm dependency . swapping vendors becomes a config change, and a deployment without the vars ships zero third-party bytes . a vendor SDK import rejected (locks the choice in at build time).
- 2026-08-19 . enrollment_confirmed and free_lesson_completed counted from the database instead of the browser . the first is a server-side change staff make, the second belongs to a button that must work without JavaScript, so no browser beacon can observe either honestly . firing them client-side anyway rejected (would report numbers that are not measurements).
- 2026-08-19 . Account deletion retains credentials (unlinked) and audit_log entries . /privacy already promises exactly this, employers holding a code must keep getting an answer, and the audit entry is the evidence the erasure happened . full purge rejected (breaks public verification and destroys the record of the deletion).
- 2026-08-19 . Deletion execution requires the requester's email typed back . it is irreversible and one mis-click reaches a real person's account . a plain confirm checkbox rejected.
- 2026-08-19 . Data requests get their own rate-limit bucket . someone exercising a legal right must not be blocked because a housemate on the same connection took the free test three times . sharing emailCapture rejected.
- 2026-08-19 . completeLessonAction returns the next href instead of redirecting . chained action redirects are dropped by the App Router once one has been performed, so lesson two onward silently needed a second click in production . server-side redirect rejected (measured broken).
- 2026-08-19 . E2E runs against a production build on a dedicated port with capped workers and one local retry . dev-server compilation, a foreign server on port 3000, and worker oversubscription each produced red runs unrelated to the code . the previous dev-server setup rejected (it hid a real production bug for two phases).
- 2026-08-19 . Credential pages deliberately excluded from sitemap.xml . they are reached by code, not by browsing, and listing them turns a lookup tool into a directory of credential holders . including them rejected.
- 2026-08-19 . Storage buckets created programmatically with the service role rather than by hand in the dashboard . it is a two-line API call, it is repeatable, and it removed the last standing human-only blocker . asking the user to click through the dashboard rejected.
- 2026-08-19 . A public talent profile requires BOTH is_public and at least one active credential . a profile here without a credential is exactly the unbacked claim the product exists to replace . opt-in alone rejected.
- 2026-08-19 . A private or nonexistent username both return 404 . distinguishing them would confirm an account exists to anyone guessing . a distinct "this profile is private" page rejected.
- 2026-08-19 . Username uniqueness enforced case-insensitively by a database index, and the format by a CHECK constraint . the URL is an identity, and two people at /talent/juana and /talent/Juana is a phishing surface . application-level validation alone rejected.
- 2026-08-19 . Images compressed to WebP in the browser before upload . mobile-first product, metered mobile data, and a 4 MB phone photo becomes about a tenth of that before it leaves the device . server-side resizing rejected (pays the upload cost anyway).
- 2026-08-19 . Playwright globalSetup warms the server, including one real server-action submission . the first action dispatch measured about two minutes cold against two seconds warm, and parallel tests all queue behind it and time out together . raising every timeout instead rejected (hides the cliff rather than removing it).
- 2026-08-21 . getSessionUser wrapped in React cache() . verifySessionCookie(cookie, true) checks revocation over the network, and one interaction asked for the user three times (layout, page, action) . leaving three round trips rejected (free to remove, and revocation is still re-checked on the next request).
- 2026-08-21 . revalidatePath removed from every server action that returns state to useActionState; cached paths are purged afterwards by a POST to /api/revalidate . measured: the action and its re-render finish server-side in under a second while the browser sits on a disabled "Saving..." button forever, 2-of-3 to 4-of-4 depending on the case, reproduced with a single call against an unrelated fully dynamic route and gone the moment the call is removed . keeping revalidatePath in the action rejected (it silently breaks every admin editor and the profile editor in production; next dev hides it).
- 2026-08-21 . Admin create/delete actions return redirectTo instead of calling redirect() . same class of App Router unreliability the lesson player already works around, and it lets the cache purge fire before the navigation . redirect() inside the action rejected.
- 2026-08-21 . /api/revalidate answers 404, not 403, to non-admins . identical reasoning to /admin 404ing . a 403 rejected (it confirms the endpoint exists).
- 2026-08-21 . revalidatePath calls targeting fully dynamic routes (/dashboard/*, /admin/*, /talent/[username], /employers, /verify/[code]) deleted rather than moved . those routes are server-rendered on every request, so the calls never did anything . keeping them "just in case" rejected (each one re-triggers the hang above).
- 2026-08-21 . An e2e test now publishes a blog post through the real admin UI . the whole admin area was broken in production for a phase and the suite never submitted an admin form, only read admin pages . database-level assertions alone rejected (they are what missed it).
- 2026-08-21 . GCash/Maya receiving details moved into NEXT_PUBLIC_PAYMENT_* env vars, with the pre-cohort copy as the fallback . the receiving account changes with the business, not with the code, and the old TODO would have needed a developer . hardcoding the numbers rejected.
- 2026-08-21 . A payment channel renders only when BOTH its name and number are set . a receiving number with no name to check it against is what a scam looks like . showing a half-filled instruction rejected.
- 2026-08-21 . router.refresh() removed from CompleteLessonButton . called immediately after router.push() in the same transition it stopped the transition from ever completing in a production build, leaving the button on "Saving..." - the client-side face of the revalidatePath finding; bisected on one build with a runtime toggle, 36.4s pass without it against a reproducible failure with it . keeping it for cache freshness rejected (the destination is server-rendered on every request).
- 2026-08-21 . Lesson completion moved from a server action to POST /api/lessons/[lessonId]/complete . removing revalidatePath and then router.refresh() each helped but the journey still failed under concurrency, and at 2 workers as well as 3, so it was not queueing - an action's response carries a re-render of the current route and that stream stalls under load even when the action's work is done; the route handler answers with JSON and the suite went 62/64 in 5.1 min to 64/64 in 2.2 . keeping the action and widening timeouts rejected (hides a cliff real learners on slow connections would hit).
- 2026-08-21 . Backup workflow pins PostgreSQL 17 from PGDG and calls /usr/lib/postgresql/17/bin/pg_dump by absolute path, sets pipefail, and verifies the archive before uploading . it had never once produced a backup - Ubuntu's client 16 refuses to dump a 17 server, and `pg_dump | gzip` exits with gzip's status, so every weekly run archived a 170-byte gzip of nothing and reported success . trusting a green checkmark rejected (a backup that looks like it works is worse than no backup).
- 2026-08-22 . Chapter quizzes are type 'chapter' with a module_id, unlimited retakes, and never issue a credential . they are formative, and a learner who has to ration attempts on a practice quiz stops using it to learn . reusing the exam type rejected (the issuance path keys off it).
- 2026-08-22 . The final assignment unlocks only when every lesson is read and every chapter quiz passed, checked in the action and not only the page . the review queue is a human reading work by hand, and it should not fill with submissions from people who skipped to the end . a page-level check alone rejected (a page is not a gate).
- 2026-08-22 . Returning or failing an assignment requires a written note . it is the only feedback the learner gets . an optional note rejected.
- 2026-08-22 . scoreAttempt normalises over the competencies an assessment actually asked about . weighting absent ones as zero scored a perfect judgment-only quiz at 20% against a 70% pass mark, making one credential unobtainable . leaving the diagnostic's fixed weights rejected (they were written for a test that always covers all four).
- 2026-08-22 . The course catalogue moved behind the login at the client's request . the landing page is now a sales page and the product is what you get for signing up . keeping it public rejected by the client, knowing it costs the organic discovery those pages had.
- 2026-08-22 . Motion via the `motion` package, not `npm create @motion-script@latest` . that package does not exist on npm, and `npm create` scaffolds a new project rather than adding to this one . running the given command rejected (it would have created a second app).
- 2026-08-22 . Every landing animation collapses to a plain fade under prefers-reduced-motion . parallax and long travel cause real nausea for some people . decorative-only treatment of the media query rejected.
- 2026-08-22 . The theme toggle offers system as a real third option, not a hidden default . a phone that switches at sunset should take the site with it, and someone who set that up should see it honoured . a two-way light/dark switch rejected.
- 2026-08-22 . Em dashes removed from user-facing text only; developer comments keep theirs . the client's objection is that it reads as machine-written to a reader, and several comments use dashes while explaining decisions that cost real time . a blanket find-and-replace rejected (a hyphen is the same tell and a comma splice is worse writing).
- 2026-08-22 . Course content is generated, then adversarially reviewed, then repaired, in three passes . the first draft of every course carried invented statistics and quizzes whose correct answer was reliably the longest option, which would let a learner score well having read nothing . single-pass generation rejected (measured 70% answer-length tell before, 12% after).
- 2026-08-22 . scripts/refresh-seed-content.ts added, dry run by default . seed.sql inserts with `on conflict do nothing` so changing authored copy never reaches a live database; that protects /admin edits and hides copy changes . making the seed itself update rejected (it would silently revert admin work on every run).
- 2026-08-22 . The question radiogroups carry aria-labelledby pointing at their own legend . the theme toggle is a radiogroup too, so an unscoped radio query matched Light/Dark/System and the exam e2e passed its visibility wait before any question rendered . leaving the group unnamed rejected (a screen reader entering it heard nothing, and the test was silently testing the wrong element).
- 2026-08-22 . The AI test results page sends its two CTAs to /signup?next=... rather than straight to the gated routes . the results page is the end of the anonymous funnel and both destinations now redirect to login, so every finisher hit an unexplained sign-in wall . linking directly rejected (the gate is correct; the dead end was not).
- 2026-08-22 . seed-courses.ts rewrites existing questions in place by sort_order instead of deleting and re-inserting . attempts.answers stores question ids with no foreign key behind them, so new ids score an in-flight attempt as zero and burn one of the learner's tries . delete-and-reinsert kept only when the question count itself changed, where no positional match exists.
- 2026-08-22 . attempt-create rate limit raised from 5/hour to 20/hour per hashed IP . a large share of this audience arrives through mobile carrier NAT where hundreds share one address, so five strangers could consume the free test for everyone behind it . emailCapture deliberately left at 3, since the email gate is the actual spam vector.
- 2026-08-22 . The em dash guard now scans scripts/ as well as src/ . seed-courses.ts built a chapter quiz title with an em dash and wrote it into assessments.title, which learners read, while the test stayed green . refresh-seed-content.ts is exempted by name because counting em dashes is its job.
- 2026-08-22 . The dashboard reports an Approved state when a submission passed but no active credential exists . approving can succeed while issuing fails, and the fall-through told a learner to start an assignment they had already passed . folding it into 'Assignment ready' rejected.
